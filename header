set -eu
set -o pipefail
export selfdir=$(dirname $0)
export self=$(basename $0 | sed 's \.sh$  ')
export tmp=/tmp/${self}.$$
trap 'rm -rf ${tmp}*' EXIT HUP INT QUIT TERM
. argenv
echo=${echo-}
test=${test:-false}
if $test; then
	echo=echo
fi
# DO NOT EDIT THIS FILE.  Auto-generated from header.md by lit.
