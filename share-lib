. header
. default infile ${file}.${in}
. option out
if test -n "$out"; then
	. default outfile ${file}.${out}
	. default outdo ${outfile}.do
else
	. default outfile ${file}
	. default outdo ${outfile}.do
fi
log exec=true $echo cp ${selfdir}/${tool}.${in}.${out}.do $outdo
log exec=true $echo chmod +x $outdo
log exec=true $echo share-adddep $outfile $outdo
# DO NOT EDIT THIS FILE.  Auto-generated from share-lib.md by lit.
