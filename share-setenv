target=$1
shift
envdir=${target}.env
if test -d "$envdir"; then
    for var in $(cd ${target}.env; ls); do
        val="$(cd ${target}.env; cat $var)"
        eval "export $var='$val'"
        log -- "$var=$val"
    done
fi
# DO NOT EDIT THIS FILE.  Auto-generated from share-setenv.md by lit.
